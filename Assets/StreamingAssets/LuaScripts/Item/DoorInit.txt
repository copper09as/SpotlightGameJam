local CE = CS.EventCenter.Instance
function DoorInit(entity)--Door初始化
    local door = entity:GetComponent("Door")
    door:AddOpenTheDoor(
            function()
                CS.UnityEngine.Debug.Log(entity.name.."开门成功")
            end
    )

end

